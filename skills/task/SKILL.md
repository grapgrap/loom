---
name: task
description: >-
  plan의 결과 계약을 사용자와의 실행 합의를 거쳐 구현하고 검증한다. plan에서 태스크를 꺼내
  작업할 때 사용한다. "이거 구현하자", "task", "이 태스크 진행" 같은 맥락에서 트리거된다.
---

# Task

plan의 태스크가 약속한 결과를 사용자와의 실행 합의를 거쳐 구현하고 검증한다.

**task는 합의 후 구현한다. 합의 없이 구현하지 않는다.**

**plan은 살아있는 문서다. 실행 중 plan이 현실과 어긋나면 plan을 맹신하지 말고 갱신한다.**

## Step 0: 컨텍스트 로드

1. 작업 대상 프로젝트의 `.loom/index.md`를 읽어 도메인 전체 지도를 파악한다.
2. 대상 plan 파일을 읽어 Plan의 목적, 전체 결과 사슬과 태스크별 진행 상태를 파악한다.
3. 대상 태스크의 목적, 수행할 변화, 제외, 완료 결과, 검증과 선행 결과를 구현 입력으로 읽는다. 제외가 없으면 별도 제약을 추론하지 않는다.
4. 대상 태스크가 요구하는 선행 결과가 성립했는지 확인한다. 선행 결과가 성립하지 않았으면 구현을 시작하지 않고 먼저 실행할 태스크를 제안한다.
5. 대상 태스크와 Plan의 목적에서 좁은 검색어를 만들고 `aeira search -s {프로젝트의 .loom 경로} "{검색어}"`로 관련 concept과 flow 후보를 찾는다. 결과가 비면 표현을 바꿔 재시도한다.
6. 후보 노드에 `aeira graph neighbors -s {프로젝트의 .loom 경로} --direction=outgoing "{노드}"`를 실행해 연결된 concept과 flow를 확장한다. 노드는 `.loom` 기준 경로 형식이다.
7. 후보 문서를 읽고 대상 태스크에 맞는 concept과 flow만 선별하여 구현의 도메인 구성을 파악한다.
8. decision은 이 단계에서 일괄로 읽지 않는다. 실행 중 발견을 처리하거나 구현 대안을 평가할 때 이전 결정을 확인해야 하는 경우에만 해당 맥락으로 `aeira search -s {프로젝트의 .loom 경로} "{검색어}"`를 실행한다. 관련 concept이나 flow가 있으면 `aeira graph neighbors -s {프로젝트의 .loom 경로} --direction=incoming "{노드}"`로 이를 참조하는 decision 후보를 보강한다. 맥락에 맞는 decision만 읽는다. 같은 사안이 충돌하면 decision concept의 최신 우선 규칙에 따라 순번이 큰 decision을 따른다.

사용자가 특정 태스크를 지정한 경우 해당 태스크에 집중한다.
지정하지 않은 경우, 선행 결과가 모두 성립한 미완료 태스크 중 다음으로 실행할 태스크를 제안한다.

## Step 1: 문서 작성 가이드 로드

`../../references/document-authoring-guide.md`를 읽는다.

decision을 기록하거나 plan을 처음 갱신하기 전에 가이드를 로드한다. 이후 task가 Loom 문서를 작성하거나 검증할 때 이 가이드를 적용한다.

## Step 2: 룰 카탈로그 로드

`.loom/rules/index.md`를 읽어 어떤 룰과 컨벤션이 있는지 카탈로그를 인지한다.

rule은 이 프로젝트에서 항상 지켜져야 하는 강제적 합의, convention은 강하게 권장되지만 대체 가능한 형태이다. 인덱스를 통해 항목 목록과 각 항목의 호출 상황을 파악한다. 본문은 활용 시점에 필요한 항목을 골라낸 뒤 로드한다.

## Step 3: 방향성 합의

대상 태스크의 완료 결과를 만드는 구현 방법의 초안을 제시한다. Plan의 완료 결과는 구현 목표이자 완료 판정 기준이다. task는 사용자와 다시 합의하지 않고 완료 결과를 바꾸지 않는다.

### 초안의 형태

- 구현이 완료 결과를 어떻게 성립시키는지 수도코드 수준으로 제시한다.
- 관련 concept의 책임과 flow의 단계가 코드나 문서의 구조에 어떻게 표현되는지 보인다.
- 구현 방법이 Plan의 수행할 변화와 제외를 어떻게 지키는지 설명한다.
- Plan에 명시된 검증을 어떤 방법으로 수행할지 설명한다.

### 논의

사용자와 논의하여 구현 방법을 구체화한다. 같은 완료 결과를 만드는 구현 대안은 이 단계에서 해소한다. 구현 방법만 달라지는 경우에는 Shape나 Plan으로 돌아가지 않는다.

방향성을 합의하기 위해 코드와 기존 문서를 확인하는 동안 Plan의 가정이 현실과 다르다고 밝혀지면 아래 "실행 중 발견의 처리"를 적용한다.

### 실행 중 발견의 처리

발견이 무엇을 바꾸도록 요구하는지에 따라 목적지를 정한다. task는 concept이나 index를 직접 변경하지 않는다.

- 발견이 concept이나 flow의 의미 또는 경계를 바꾸도록 요구하면 구현을 멈추고 shape로 돌아갈지 사용자와 합의한다. 변경의 크기는 라우팅 기준이 아니다.
- 구현 방법을 정하기 위해 비자명한 기술적 선택이 필요하면 대안과 트레이드오프를 사용자와 검토한다. 사용자가 선택에 동의하면 문서 작성 가이드와 decision concept에 따라 decision을 기록한다. 템플릿은 `../../templates/decision.md`다. 파일 이름은 `{NNNN}-{결정-제목}.md` 형식을 사용한다.
- 발견이 현재 태스크의 수행할 변화, 제외, 완료 결과나 검증을 바꾸도록 요구하면 구현을 멈춘다. 변경할 결과 계약을 사용자와 합의한 뒤 현재 태스크를 갱신하고 구현을 재개한다.
- 발견이 후속 태스크의 수행할 변화, 제외, 완료 결과, 검증이나 결과 의존을 바꾸도록 요구하면 영향받는 Plan 위치를 즉시 갱신하고 변경된 결과 사슬을 사용자에게 알린다.
- 기술적 선택의 결과가 남은 실행을 제약하면 영향받는 Plan 태스크나 결과 의존에 그 제약을 반영한다. 선택의 근거는 decision에만 기록한다.
- 코드에서 확인할 수 있고 남은 실행에 영향을 주지 않는 구현 상세는 Plan에 기록하지 않는다.

실행 중 발견을 시간순으로 누적하는 전역 진행 맥락은 만들지 않는다.

### 임시 핸드오프

사용자가 명시적으로 요청하고 현재 태스크가 완료되지 않았으면 현재 태스크에 임시 핸드오프를 작성한다. 사용자가 요청하지 않으면 task가 필요성을 추론하여 작성하지 않는다.

임시 핸드오프에는 다음 실행자가 이어서 수행할 재개 지점과 아직 끝나지 않은 작업만 기록한다. 태스크 실행을 재개하거나 태스크를 완료하면 임시 핸드오프를 제거한다.

### 합의 완료의 신호

사용자가 구현 방법과 검증 방법에 동의하고 실제 파일을 변경하도록 요청하면 방향성 합의가 완료된다.

## Step 4: 구현

합의한 구현 방법에 따라 코드나 문서를 변경한다.

### 구현 방식

사용자의 기호에 따라 실제 파일을 변경하거나 대화에서 초안을 제시한다. 기존 코드와 문서의 컨벤션을 따르되, 사용자의 선호를 우선한다.

### 룰 카탈로그 활용

구현할 때 인지한 룰 카탈로그를 도구 선택 절차로 활용한다:

1. 지금 수행하려는 행동을 의식적으로 명시한다
2. 카탈로그에서 그 행동에 적용될 가능성 있는 항목을 골라낸다
3. 골라낸 항목의 본문 파일을 로드해 적용 방식을 파악한다
4. 본문에 따라 코드나 문서를 변경한다

### 반복

사용자의 피드백에 따라 구현을 교정한다. 구현 중 새로운 사실이 드러나면 Step 3의 "실행 중 발견의 처리"를 즉시 적용한다. Plan 갱신을 구현 완료 시점까지 미루지 않는다.

## Step 5: 결과 검증과 완료

Plan에 명시된 검증과 변경 유형에 맞는 검사를 실행한다. 검증 결과를 완료 결과와 대조하고 제외한 범위가 바뀌지 않았는지 확인한다.

검증이 실패하거나 완료 결과가 성립하지 않았으면 태스크를 완료 처리하지 않는다. 작업을 중단할 때 사용자가 명시적으로 요청하면 Step 3에 따라 임시 핸드오프를 작성한다.

완료 결과가 성립하면 실행 중 발견이 남은 결과 사슬에 빠짐없이 반영되었는지 확인한다. 반영할 사실이 없으면 태스크의 결과 계약과 결과 의존을 유지한다. 확인을 마친 뒤 진행 상태를 완료로 갱신하고 현재 태스크의 임시 핸드오프를 제거한다.

### 작성 결과 검증

Plan의 완료 처리를 마친 뒤 이번 task에서 생성하거나 수정한 모든 Loom 문서를 문서 작성 가이드에 따라 검증한다. 가이드를 충족하지 못한 문서는 교정하고 다시 검증한다. 모든 문서가 검증을 통과한 뒤 그래프를 갱신한다.

### 그래프 갱신

`.loom/` 내 문서를 생성하거나 수정한 후 `aeira sync -s {프로젝트의 .loom 경로}` 를 실행한다.

## Summary

태스크 구현 완료 시 다음 형식으로 정리한다:

```
## Task Summary
- 태스크: [수행한 태스크 요약]
- 방향성: [합의된 접근 방식 한 줄]
- 완료 결과: [구현으로 성립한 결과]
- 검증: [실행한 검증과 결과]
- 산출물: [생성/수정된 파일 목록]
- Decisions: [기록된 decision 파일 목록, 없으면 생략]
- Plan 상태: [완료 처리한 태스크와 실행 중 갱신한 결과 사슬]
```

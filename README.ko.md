# loom

요구사항을 설계로, 설계를 실행으로 구체화하는 Claude Code·Codex 플러그인.

[English](README.md) [한국어](README.ko.md)

## 왜 만들었는가

소프트웨어를 만들 때 우리는 이미 자연스러운 흐름을 따릅니다.
요구사항을 구체화하고, 계획을 세우고, 구현하고, 돌아보는 것. loom은 이 흐름을 구조화하여 재현 가능하게 만듭니다.

구조화된 흐름에서 나오는 산출물(설계 문서, 결정 기록, 규칙)은 시간이 지나도 유효해야 합니다.
loom은 피드백 루프를 통해 이 산출물들이 코드와 함께 살아있는 상태를 유지하도록 합니다.

## 워크플로우

loom은 다섯 가지 스킬로 동작합니다.

### 작업 흐름

**shape** -> **plan** -> **task** -> 구현

- **shape**: 모호한 요구사항을 설계 수준으로 구체화합니다. 접근 전략, 제약, 트레이드오프를 다루며, concept과 flow, decision을 산출합니다. 전제·제약이 흔들렸으나 기존 설계를 유지한 경우 유지 decision을 생산합니다.
- **plan**: shape의 산출물을 실행 가능한 태스크와 의존관계로 분해하고, 확정 전 각 태스크를 사용자와 함께 검증합니다.
- **task**: plan의 태스크를 사용자와의 실행 합의를 거쳐 구현합니다. 방향성 초안을 제시하고 논의한 후 코드를 작성합니다. 작업 중 plan의 가정이 현실과 어긋나면 plan을 맹신하지 않고 갱신합니다.

### 피드백 루프

- **review**: 같은 세션의 산출물을 적대적으로 검토합니다. 세션 맥락은 범위를 식별하는 데만 사용하고, raw 파일·diff·기록된 합의로 비판을 입증합니다. 구현뿐 아니라 shape·plan·task 어느 단계 뒤에도 사용합니다.
- **calibrate**: 축적된 concept과 flow의 목적이 프로젝트 목표에 여전히 부합하는지 감사합니다. 발견을 리포트로 정리할 뿐 직접 교정하지 않으며, shape 재진입 여부는 사용자에게 맡깁니다.

## 문서 품질

loom은 자신이 작성하는 문서를 위한 내장 문서 작성 가이드를 제공합니다.
가이드는 문서 유형별 책임과 내용의 목적지를 정의하고, 위키링크를 구조적 관계로 사용하며, 언어 중립적인 기술문서 작성 기준을 제공합니다.

shape·plan·task는 Loom 문서를 변경하기 전에 가이드를 읽고, 작성을 마치면 자신이 변경한 문서를 가이드에 따라 검증합니다.
review는 확정한 범위에 Loom 문서가 있을 때 같은 가이드를 적용하되, 검토 범위를 `.loom/` 전체 감사로 넓히지 않습니다.
calibrate는 목적 정렬에 집중하며 가이드를 적용하지 않습니다.

템플릿은 문서 구조와 최소한의 섹션별 입력 안내를 제공합니다.
문서 유형을 가로지르는 판단 기준은 템플릿에 복제하지 않고 가이드가 소유합니다.

## 컴포넌트

loom은 `.loom/` 디렉토리에 여섯 가지 엔티티를 관리합니다.

### Concept

프로젝트를 이루는 도메인을 불변 수준에서 정의하는 영속 문서입니다.
"이 시스템이 무엇을 다루는가"를 기술하며, 구현이 아닌 도메인에 집중합니다.
프로젝트의 공유 어휘(ubiquitous language) 역할을 합니다.

### Flow

여러 도메인을 불러 조합하는 오케스트레이션을 기술하는 영속 문서입니다.
concept이 도메인을 정의한다면(분해), flow는 그 도메인들을 불러 조합합니다(조합). 둘은 같은 불변 수준의 두 축입니다.
가르는 질문 하나로 나눕니다 — 여러 도메인을 불러 조합하면 flow, 한 도메인 안의 동작이나 여러 입력을 한 결과로 닫는 계산이면 concept입니다. 명명 가능성은 판별 근거가 아니며, 오케스트레이션은 concept에 흡수되지 않고 flow에 남습니다. flow는 불변 수준에 머물고, 제어 상세는 코드로 내립니다.

### Decision

concept과 flow의 형태에 영향을 준 결정의 맥락을 기록합니다.
왜 이렇게 결정했는지를 보존하여, 미래에 재검토할 수 있는 근거를 제공합니다.
각 decision은 자기완결적이며 append-only입니다 — 옛 것은 수정하지 않고 새로 덧붙이고, 다른 decision을 참조해도 그것 없이 이해됩니다.
유지 decision은 전제나 제약이 흔들렸음에도 기존 concept이나 결정을 유지한 근거를 보존합니다.

### Rule

이 프로젝트에서 항상 지켜져야 하는 비기능 요구사항입니다.
강제성 축의 무거운 자리이며, shape·plan·task가 자동으로 참조하여 산출물에 반영합니다.

### Convention

강하게 권장되지만 대체 가능한 형태입니다.
표현·구조·도구의 합의된 통일을 다루며, 강제성 축의 가벼운 자리입니다.

### Plan

설계를 실행 가능한 태스크와 의존관계로 구조화하고, 작성과 실행에 걸쳐 갱신되는 살아있는 문서입니다.
shape의 산출물을 구현 단위로 분해하고, 확정 전 각 태스크를 사용자와 함께 검증하며, 진행 상황을 추적합니다. 실행 단계에서는 task가 진행 맥락(핸드오프, 작업 중 알게 된 사실)을 plan에 누적하여, 작업이 세션을 가로질러 누락 없이 이어지게 합니다.

## 시작하기

### 사전 요구

- [aeira](https://github.com/grapgrap/aeira) -- `npm install -g @grapgrap/aeira`

### 설치

#### Claude Code

```bash
claude plugin marketplace add grapgrap/loom
claude plugin install loom@loom-marketplace
```

#### Codex

```bash
codex plugin marketplace add grapgrap/loom
codex plugin add loom@loom-marketplace
```

설치한 스킬을 사용할 수 있도록 새 Claude Code 또는 Codex 세션을 시작합니다.

### 첫 사용

프로젝트에서 설계가 필요한 요구사항이 있을 때 shape부터 시작합니다. Claude Code에서는 `/loom:skill`, Codex CLI와 IDE에서는 `$loom:skill`로 명시적으로 호출합니다. ChatGPT 데스크톱 앱에서는 `@`로 Loom이나 포함된 스킬을 선택합니다.

```text
/loom:shape 사용자 인증 기능을 추가하고 싶습니다
$loom:shape 사용자 인증 기능을 추가하고 싶습니다
```

shape이 완료되면 plan으로 이어갈지, 바로 구현할지 안내합니다.

plan이 완료되면 task로 태스크를 실행합니다.

```text
/loom:task
$loom:task
```

shape·plan·task 뒤에는 같은 세션에서 review를 실행합니다. 그래야 세션 이력으로 실제 작업 범위를 식별할 수 있으며, 비판 자체는 대화의 의도가 아니라 raw 산출물로 입증합니다.

```text
/loom:review
$loom:review
```

더 강한 독립 감사에는 다른 세션을 열고 commit·range·patch 또는 산출물 범위를 명시합니다. 이전 세션의 범위를 작업 트리 상태만으로 추정하지 않습니다.

축적된 설계가 프로젝트 목표에서 멀어졌다고 느껴지면 calibrate로 감사합니다.

```text
/loom:calibrate
$loom:calibrate
```
